import { ColumnDrag } from './column-drag';

describe('ColumnDrag', () => {
  it('should create an instance', () => {
    const directive = new ColumnDrag();
    expect(directive).toBeTruthy();
  });
});
