import { MyButtonDirrective } from './my-button-dirrective';

describe('MyButtonDirrective', () => {
  it('should create an instance', () => {
    const directive = new MyButtonDirrective();
    expect(directive).toBeTruthy();
  });
});
